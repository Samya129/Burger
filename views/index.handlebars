<h1>Ooo-da-Burger</h1>
<div class="burgerContainer">
  <h2>Burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<img class="rounded mx-auto d-block text-center" style="width: 20rem; height: 20rem;display: inline-block; vertical-align: middle;" src="/assets/images/burger.png" alt="Burger Image">
<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>
</div>

<h2>Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <input type="text" id="bur" name="name">
  </div>


  <button type="submit">Add a Burger</button>
</form>