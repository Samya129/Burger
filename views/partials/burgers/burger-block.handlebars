<li class="d-flex flex-column">
	{{burger_name}}

	<button class="change-devoured btn btn-success btn-small" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}} Devour!{{else}}Care for another?{{/if}}
	</button>

	<button class="delete-burger btn btn-success btn-sm" data-id="{{this.id}}">I'm Done!</button>
</li>